<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

<main>

  <% if (session.login) { %>
    Hi, <%= session.user.name %>
    <% } %>
</main>

</body>
</html>
